<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
	<Dialog id="commentDialogId" title="{i18n>addNote}" contentHeight="25%" contentWidth="25%">
		<VBox class="sapUiMediumMargin">
			<Label text="{i18n>note}"/>
			<TextArea width="100%" placeholder="{i18n>addNote}" value="{confirmationModel>/comment}"/>
		</VBox>
		<beginButton>
			<Button type="Accept" press="onAddComment" text="{i18n>submitButton}"/>
		</beginButton>
		<endButton>
			<Button type="Reject" press="onCloseCommentDialog" text="{i18n>cancelButton}" />
		</endButton>
	</Dialog>
</core:FragmentDefinition>